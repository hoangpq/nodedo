# query schema for graphql

type Tea {
    name: String
    steepingTime: Int
    relate(name: String): [Tea]
}

type Store {
    teas: [Tea]
    tea(name: String!): Tea
}

type Query {
    store: Store
}

schema {
    query: Query
}
